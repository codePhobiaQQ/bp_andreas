{
  "routes": [
    {
      "method": "POST",
      "path": "/user/logged",
      "handler": "users.logged",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/user/change",
      "handler": "users.change",
      "config": {
        "policies": []
      }
    }
  ]
}
